# Copie este arquivo para ".env" e preencha com seus dados reais.
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=sua_senha_do_mysql
DB_NAME=agenda_ifrs

# Chave usada para assinar a sessão de login (troque por um texto aleatório)
SESSION_SECRET=troque-por-uma-frase-secreta-qualquer

PORT=3000
